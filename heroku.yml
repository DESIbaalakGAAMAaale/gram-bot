build:

  docker:

    web: Dockerfile

    worker: Dockerfile



run:

  web: <yahan aapki web command likhein>

  worker:

    command:

      - bash start.sh

    image: worker

    
